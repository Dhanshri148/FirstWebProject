@using HealthifyMe.Web.Areas.DietitionDetails.ViewModels
@model ShowDietAndExerciseViewModel

@{
}

<h1>The Exercises</h1>

<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group">
                <label asp-for="DietitionId" class="control-label"></label>
                <select asp-for="DietitionId" class="form-control" asp-items="ViewBag.DietitionId"></select>
            </div>

            <button type="submit" class="btn btn-primary">Get exercises</button>
        </form>
    </div>
</div>


@if (Model != null && Model.Exercises != null)
{
    <form method="post">
        <input type="hidden" asp-for="DietitionId" />

        <div class="card-columns mt-3">
            @foreach (var item in Model.Exercises)
            {
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title"> @item.ExerciseName </h4>
                        <p class="card-text"> @item.ExerciseCategory </p>
                    </div>
                    <div class="card-footer">

                        <button class="btn btn-sm btn-primary" type="submit"
                        asp-area="DietitionDetails" asp-controller="Home"
                        asp-action="AssignDietition" asp-route-id="@item.ExerciseId">
                            Assign Diettition
                        </button>
                    </div>
                </div>
            }
        </div>
    </form>
}



@section HeadScripts {
    <style>
        h1 {
            color: darkgreen;
            text-align: center;
            font-family: 'Brush Script MT', Arial;
            font-size: 3rem;
            font-weight: bold;
        }
    </style>
}